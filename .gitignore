# Terraform
*.tfvars
.terraform
.terraform.lock.hcl
*.tfstate
*.tfstate.*

# Credentials / secrets (defense-in-depth — none are committed)
.env
.env.*
*.pem
*.key
*-credentials.json
service-account*.json

# Go / build output
vmup
vmup-*
dist/

# Local tooling
.claude/
